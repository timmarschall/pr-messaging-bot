# --- GitHub App Configuration ---
# Numeric App ID from GitHub settings
APP_ID=
# Private key (either literal, or path you load separately). Prefer loading via file in code.
# If embedding multiline key, escape newlines \n or use environment file loaders that support multiline.
PRIVATE_KEY=
# Webhook secret you set in the App settings (use a strong random string in production)
WEBHOOK_SECRET=development

# --- Logging ---
# trace | debug | info | warn | error
LOG_LEVEL=debug

# --- Development Webhook Proxy (Smee or similar) ---
# Example: https://smee.io/XXXXXXXXXXXX
WEBHOOK_PROXY_URL=

# --- Slack Integration (required for messaging; if blank Slack sync is skipped) ---
SLACK_TOKEN=
# Channel ID (e.g. C0123ABCDEF). Not the channel name.
SLACK_CHANNEL=

# --- Storage / Caching ---
# Max in-memory entries for message timestamp cache (older entries evicted FIFO)
STORAGE_MAX_ENTRIES=500

# --- Optional GitHub API Overrides ---
# Set to increase pagination limits or enterprise host. Usually leave unset.
GITHUB_API_BASE_URL=
GITHUB_GRAPHQL_URL=

# --- Misc ---
# Set to "true" to enable extra verbose temporary diagnostics (if implemented later)
EXPERIMENTAL_DIAGNOSTICS=false

# Note: Never commit real secrets. Copy this file to .env and fill values locally.